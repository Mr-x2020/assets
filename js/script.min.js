function submitForm() {
  var button = document.querySelector('.btn.btn-primary');
  var spinner = '<span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>' +
               'Submitting...';

  // Disable the button and replace its content with the spinner
  button.disabled = true;
  button.innerHTML = spinner;

  var e = document.getElementById("myForm"),
      t = new FormData(e),
      s = new XMLHttpRequest();

  s.open(
    "POST",
    "https://script.google.com/macros/s/AKfycbyRiGTZCjojfjZJS-Oi1pbxYmRBdCZVa-m_qhHpqMvqWHG9HpDmT5tfVRG86f0aGY7e/exec"
  );

  s.onload = function () {
    if (s.status === 200) {
      document.getElementById("successMessage").className = ".d-block alert alert-success";
      resetFormInputs();
    } else {
      document.getElementById("successMessage").className = ".d-block alert alert-warning";
    }

    // Enable the button and revert its content back to original
    button.disabled = false;
    button.innerHTML = '<i class="fa fa-paper-plane"></i> Submit';
  };

  s.send(t);
  return false;
}
